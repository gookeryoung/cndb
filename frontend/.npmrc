; 统一走 npmmirror 镜像（lockfile 中的 tarball URL 即此域名）。
; pnpm 12 供应链校验会用 lockfile URL 与当前 registry 元数据比对，
; CI 若用默认 npmjs 官方源会因 URL 不一致报 ERR_PNPM_LOCKFILE_RESOLUTION_VERIFICATION。
registry=https://registry.npmmirror.com/

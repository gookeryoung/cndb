diff --git a/es/isEqual.js b/es/isEqual.js
index 703e47a8b7114025a166ecdbf765ee1aa37f4fbf..77e0f369e39a371cb3251bcccc6c61ae46fd7e88 100644
--- a/es/isEqual.js
+++ b/es/isEqual.js
@@ -3,6 +3,18 @@ import warning from "./warning";
 
 /**
  * Deeply compares two object literals.
+ *
+ * 修复说明（2026-09）：
+ * 原实现使用全局 refSet 记录已访问过的 `a` 节点，但只在进入时 add、从不移除，
+ * 导致 DAG（同一子对象通过多条属性路径可达）被误判为循环引用并持续打印警告。
+ *
+ * 修复改为「祖先集合」语义：
+ *   1. 先做 a === b 身份短路（避免 refSet 记录冗余的已命中节点）
+ *   2. 用 `ancestors` 集合只追踪**当前递归路径上的祖先**，而非所有曾访问过的节点
+ *   3. 进入子节点时 add，回溯时 delete —— 这样只有真循环（节点出现在自己的祖先链上）才触发警告
+ *
+ * 保持原函数签名与行为（真循环仍视为不相等并打印 warning）。
+ *
  * @param obj1 object 1
  * @param obj2 object 2
  * @param shallow shallow compare
@@ -10,29 +22,41 @@ import warning from "./warning";
  */
 function isEqual(obj1, obj2) {
   var shallow = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
-  // https://github.com/mapbox/mapbox-gl-js/pull/5979/files#diff-fde7145050c47cc3a306856efd5f9c3016e86e859de9afbd02c879be5067e58f
-  var refSet = new Set();
   function deepEqual(a, b) {
-    var level = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 1;
-    var circular = refSet.has(a);
+    var ancestors = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
+    var level = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 1;
+
+    // 身份完全相同：短路返回相等，不触碰祖先集合
+    if (a === b) {
+      return true;
+    }
+
+    // 只追踪「当前递归路径上的祖先」——节点已在祖先链上 = 真循环
+    var circular = ancestors.includes(a);
     warning(!circular, 'Warning: There may be circular references');
     if (circular) {
       return false;
     }
-    if (a === b) {
-      return true;
-    }
+
     if (shallow && level > 1) {
       return false;
     }
-    refSet.add(a);
+
+    var nextAncestors;
+    var shouldTrack = a && typeof a === 'object';
+    if (shouldTrack) {
+      nextAncestors = ancestors.concat(a);
+    } else {
+      nextAncestors = ancestors;
+    }
+
     var newLevel = level + 1;
     if (Array.isArray(a)) {
       if (!Array.isArray(b) || a.length !== b.length) {
         return false;
       }
       for (var i = 0; i < a.length; i++) {
-        if (!deepEqual(a[i], b[i], newLevel)) {
+        if (!deepEqual(a[i], b[i], nextAncestors, newLevel)) {
           return false;
         }
       }
@@ -44,12 +68,12 @@ function isEqual(obj1, obj2) {
         return false;
       }
       return keys.every(function (key) {
-        return deepEqual(a[key], b[key], newLevel);
+        return deepEqual(a[key], b[key], nextAncestors, newLevel);
       });
     }
     // other
     return false;
   }
-  return deepEqual(obj1, obj2);
+  return deepEqual(obj1, obj2, [], 1);
 }
 export default isEqual;
diff --git a/lib/isEqual.js b/lib/isEqual.js
index af34622fe2535fbeb037f6a859e4ceb165147fa4..eafd5045ee374c5fce08bf41febec8f2f24e3705 100644
--- a/lib/isEqual.js
+++ b/lib/isEqual.js
@@ -1,14 +1,16 @@
 "use strict";
 
 var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
-Object.defineProperty(exports, "__esModule", {
-  value: true
-});
+Object.defineProperty(exports, "__esModule", { value: true });
 exports.default = void 0;
 var _typeof2 = _interopRequireDefault(require("@babel/runtime/helpers/typeof"));
 var _warning = _interopRequireDefault(require("./warning"));
+
 /**
  * Deeply compares two object literals.
+ *
+ * 修复说明：把「全局 refSet」改成「祖先链」追踪，消除 DAG 误判为循环引用的警告。
+ *
  * @param obj1 object 1
  * @param obj2 object 2
  * @param shallow shallow compare
@@ -16,46 +18,38 @@ var _warning = _interopRequireDefault(require("./warning"));
  */
 function isEqual(obj1, obj2) {
   var shallow = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
-  // https://github.com/mapbox/mapbox-gl-js/pull/5979/files#diff-fde7145050c47cc3a306856efd5f9c3016e86e859de9afbd02c879be5067e58f
-  var refSet = new Set();
   function deepEqual(a, b) {
-    var level = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 1;
-    var circular = refSet.has(a);
+    var ancestors = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
+    var level = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 1;
+
+    // 身份相同短路
+    if (a === b) return true;
+
+    var circular = ancestors.includes(a);
     (0, _warning.default)(!circular, 'Warning: There may be circular references');
-    if (circular) {
-      return false;
-    }
-    if (a === b) {
-      return true;
-    }
-    if (shallow && level > 1) {
-      return false;
-    }
-    refSet.add(a);
+    if (circular) return false;
+
+    if (shallow && level > 1) return false;
+
+    var nextAncestors = a && typeof a === 'object' ? ancestors.concat(a) : ancestors;
     var newLevel = level + 1;
+
     if (Array.isArray(a)) {
-      if (!Array.isArray(b) || a.length !== b.length) {
-        return false;
-      }
+      if (!Array.isArray(b) || a.length !== b.length) return false;
       for (var i = 0; i < a.length; i++) {
-        if (!deepEqual(a[i], b[i], newLevel)) {
-          return false;
-        }
+        if (!deepEqual(a[i], b[i], nextAncestors, newLevel)) return false;
       }
       return true;
     }
     if (a && b && (0, _typeof2.default)(a) === 'object' && (0, _typeof2.default)(b) === 'object') {
       var keys = Object.keys(a);
-      if (keys.length !== Object.keys(b).length) {
-        return false;
-      }
+      if (keys.length !== Object.keys(b).length) return false;
       return keys.every(function (key) {
-        return deepEqual(a[key], b[key], newLevel);
+        return deepEqual(a[key], b[key], nextAncestors, newLevel);
       });
     }
-    // other
     return false;
   }
-  return deepEqual(obj1, obj2);
+  return deepEqual(obj1, obj2, [], 1);
 }
 var _default = exports.default = isEqual;
